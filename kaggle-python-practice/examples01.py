# variable assignment and initialization (int) -> a comment in python
# the = (assignment) oparators is used to assign a value to a variable
spam_amount = 0 
print(spam_amount)

# type in-built-function returns the type-class of the object passed as argument
print(type(spam_amount))

# Ordering Spam,egg, Spam, Spam, bacon and Spam(4 more serving of Spam)
#  the + (addition) oparators adds two numbers and concatinates strings
spam_amount = spam_amount + 4

# condtional statement does something when condition is true
if spam_amount > 0:
    # function call (in build print function to print output)
    print("But I don't want any spam!") 


# the * (asterisk) operator performs multiplication on numbers and strings
viking_song = "Spam " * spam_amount
print(viking_song)